---
plan: 03-01
phase: 3
title: Triển khai Data Layer và Logic tiến hóa
wave: 1
depends_on: [02-02]
requirements: [DATA-01, DATA-02]
files_modified:
  - src/data/save_manager.py
  - src/data/__init__.py
  - src/main.py
  - src/ui/pet_window.py
autonomous: true
---

# Plan 03-01: Data Layer & Evolution Logic

## Goal

Xây dựng hệ thống lưu trữ điểm số (Energy, Level) vào file JSON local để bảo toàn tiến trình của người dùng sau mỗi lần tắt/mở ứng dụng. Tích hợp logic "tiến hóa" (lên cấp) khi đạt đủ điểm Energy, và hiển thị thanh tiến trình (Progress Bar) cơ bản trên UI.

## Tasks

### Task 1: Xây dựng SaveManager

<action>
Tạo `src/data/__init__.py` rỗng và `src/data/save_manager.py`:

```python
import json
import os

class SaveManager:
    """
    Quản lý lưu trữ tiến trình (Energy, Level) vào file JSON cục bộ.
    """
    def __init__(self, save_path: str = None):
        if save_path is None:
            # Lưu ở thư mục app data của user hoặc thư mục hiện tại
            base_dir = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
            self.save_path = os.path.join(base_dir, "save_data.json")
        else:
            self.save_path = save_path

        self.data = {
            "level": 1,
            "energy": 0,
            "max_energy_for_next_level": 100
        }
        self.load()

    def load(self):
        """Đọc dữ liệu từ file JSON."""
        if os.path.exists(self.save_path):
            try:
                with open(self.save_path, "r", encoding="utf-8") as f:
                    loaded = json.load(f)
                    # Cập nhật các key có sẵn để tránh mất default values nếu file cũ
                    self.data.update(loaded)
            except (json.JSONDecodeError, IOError):
                print("[Data] File save bị lỗi, dùng dữ liệu mặc định.")

    def save(self):
        """Lưu dữ liệu hiện tại xuống file JSON."""
        try:
            with open(self.save_path, "w", encoding="utf-8") as f:
                json.dump(self.data, f, indent=4)
        except IOError as e:
            print(f"[Data] Lỗi lưu file: {e}")

    def add_energy(self, amount: int) -> bool:
        """
        Cộng/trừ năng lượng. Nếu đạt max_energy, tăng level.
        Trả về True nếu có level up.
        """
        self.data["energy"] += amount
        
        # Không để energy âm
        if self.data["energy"] < 0:
            self.data["energy"] = 0

        level_up = False
        while self.data["energy"] >= self.data["max_energy_for_next_level"]:
            self.data["energy"] -= self.data["max_energy_for_next_level"]
            self.data["level"] += 1
            # Cấp sau cần nhiều năng lượng hơn 20%
            self.data["max_energy_for_next_level"] = int(self.data["max_energy_for_next_level"] * 1.2)
            level_up = True

        self.save()
        return level_up

    @property
    def level(self) -> int:
        return self.data["level"]

    @property
    def energy(self) -> int:
        return self.data["energy"]

    @property
    def max_energy(self) -> int:
        return self.data["max_energy_for_next_level"]
```
</action>

<read_first>
- Không có file cần đọc trước, tạo mới thư mục `src/data/`.
</read_first>

<acceptance_criteria>
- `src/data/save_manager.py` chứa class `SaveManager`.
- File chứa logic tính toán cấp độ (`add_energy`).
- Hệ số tăng `max_energy` sau mỗi level là 1.2 (tăng 20%).
- `energy` không được rơi xuống dưới 0.
- Gọi `self.save()` tự động mỗi khi `add_energy` được thực thi.
</acceptance_criteria>

---

### Task 2: Thêm Progress Bar (Energy Bar) vào UI

<action>
Cập nhật `src/ui/pet_window.py` để thêm thanh năng lượng phía dưới Pet.

1. Import `QProgressBar` từ `PyQt6.QtWidgets`.
2. Sửa `_setup_ui` để thêm:
```python
        # ---- Energy Bar ----
        from PyQt6.QtWidgets import QProgressBar
        self.energy_bar = QProgressBar(self)
        self.energy_bar.setGeometry(10, self.WINDOW_SIZE - 20, self.WINDOW_SIZE - 20, 10)
        self.energy_bar.setTextVisible(False)
        self.energy_bar.setStyleSheet("""
            QProgressBar {
                border: 1px solid #45475a;
                border-radius: 5px;
                background-color: #1e1e2e;
            }
            QProgressBar::chunk {
                background-color: #a6e3a1; /* Xanh lá */
                border-radius: 4px;
            }
        """)

        # ---- Level Label ----
        self.level_label = QLabel("Lv.1", self)
        self.level_label.setAlignment(Qt.AlignmentFlag.AlignCenter)
        self.level_label.setStyleSheet("color: #f9e2af; font-weight: bold; font-size: 12px; background: transparent;")
        self.level_label.setGeometry(0, self.WINDOW_SIZE - 40, self.WINDOW_SIZE, 20)
```

3. Thêm method `update_stats` vào `PetWindow`:
```python
    def update_stats(self, level: int, energy: int, max_energy: int):
        """Cập nhật thanh năng lượng và cấp độ."""
        self.level_label.setText(f"Lv.{level}")
        self.energy_bar.setMaximum(max_energy)
        self.energy_bar.setValue(energy)
```
</action>

<read_first>
- `src/ui/pet_window.py` (để biết chính xác vị trí chèn code)
</read_first>

<acceptance_criteria>
- `pet_window.py` chứa `self.energy_bar` (QProgressBar).
- `pet_window.py` chứa `self.level_label`.
- Chứa method `update_stats(level, energy, max_energy)`.
</acceptance_criteria>

---

### Task 3: Tích hợp SaveManager vào Entry Point

<action>
Cập nhật `src/main.py`:

1. Import `SaveManager` từ `data.save_manager`.
2. Khởi tạo `SaveManager` trong hàm `main()`.
3. Gọi `window.update_stats` ngay sau khi khởi tạo để hiển thị mức điểm cũ.
4. Cập nhật hàm `on_status_changed` để cộng điểm và kiểm tra tiến hóa:
```python
        if response:
            # Cộng/trừ điểm
            level_up = save_mgr.add_energy(response.energy_change)
            
            # Nếu level up, thay đổi thông điệp
            if level_up:
                window.update_mood("Evolving", "Tui vừa tiến hóa nè! Đỉnh quá Tính ơi!")
            else:
                window.update_mood(response.status, response.message)
                
            # Cập nhật thanh Energy
            window.update_stats(save_mgr.level, save_mgr.energy, save_mgr.max_energy)
```
</action>

<read_first>
- `src/main.py`
- `src/data/save_manager.py` (để dùng đúng method signatures)
</read_first>

<acceptance_criteria>
- `src/main.py` import `SaveManager`.
- Biến `save_mgr = SaveManager()` được khởi tạo.
- UI gọi `update_stats` ngay lúc khởi động app.
- Khi AI trả về response có `energy_change`, điểm được lưu lại.
- Xử lý được event "tiến hóa" (level_up = True).
</acceptance_criteria>

## Verification

Chạy app:
```bash
cd src
python main.py
```

**Kết quả mong đợi:**
1. Có thêm thanh bar màu xanh lá mỏng ở dưới bé Slime.
2. Có dòng chữ "Lv.1" hiện phía trên thanh bar.
3. Khi bạn làm việc và AI trả về `Energy: +8`, thanh màu xanh tăng lên, điểm lưu vào `src/save_data.json`.
4. Tắt app, bật lại, thanh năng lượng và level vẫn giữ nguyên, không bị reset về 0.

## must_haves

- [ ] Data layer không bị lỗi nếu thư mục cài đặt thay đổi.
- [ ] Thanh ProgressBar hoạt động không crash.
- [ ] Khi level up, status ép buộc thành `Evolving` với emoji `🌟`.
