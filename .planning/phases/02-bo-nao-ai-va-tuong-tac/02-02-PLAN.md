---
plan: 02-02
phase: 2
title: Cập nhật UI - Speech Bubble & Sprite theo Mood
wave: 2
depends_on: [02-01]
requirements: [UI-02, UI-03]
files_modified:
  - src/ui/pet_window.py
autonomous: true
---

# Plan 02-02: Speech Bubble & Sprite Switching

## Goal

Cập nhật PetWindow để hiển thị bóng thoại (Speech Bubble) chứa message từ AI và thay đổi emoji/sprite của bé Slime dựa trên trạng thái cảm xúc (Happy/Sad/Angry/Evolving).

## Tasks

### Task 1: Định nghĩa Sprite Map theo Mood

<action>
Mở file `src/ui/pet_window.py` và thêm dict MOOD_SPRITES vào đầu class:

```python
# Sprite map theo mood (emoji placeholder — Phase 3 sẽ thay bằng ảnh thật)
MOOD_SPRITES = {
    "Happy":    "🟢",   # Xanh lá — vui vẻ, tập trung tốt
    "Sad":      "🔵",   # Xanh dương — buồn, nhớ nhà
    "Angry":    "🔴",   # Đỏ — tức giận vì bị xao nhãng
    "Evolving": "🌟",   # Sao vàng — đang tiến hóa!
}
```
</action>

<read_first>
- `src/ui/pet_window.py` (phiên bản hiện tại từ Phase 1)
</read_first>

<acceptance_criteria>
- `pet_window.py` chứa dict `MOOD_SPRITES` với 4 keys: Happy, Sad, Angry, Evolving.
</acceptance_criteria>

---

### Task 2: Thêm Speech Bubble Widget

<action>
Thêm Speech Bubble vào `_setup_ui` trong `PetWindow` và thêm method `update_mood`:

Cập nhật method `_setup_ui` để thêm speech bubble bên trên Pet:

```python
def _setup_ui(self):
    """Thiết lập các thành phần UI bên trong cửa sổ."""
    # ---- Speech Bubble ----
    self.bubble_label = QLabel("", self)
    self.bubble_label.setWordWrap(True)
    self.bubble_label.setAlignment(Qt.AlignmentFlag.AlignCenter)
    self.bubble_label.setStyleSheet("""
        QLabel {
            background-color: rgba(30, 30, 46, 220);
            color: #cdd6f4;
            border: 1px solid #89b4fa;
            border-radius: 10px;
            padding: 6px 10px;
            font-family: 'Segoe UI', Arial;
            font-size: 11px;
            font-weight: 500;
        }
    """)
    self.bubble_label.setGeometry(0, -70, self.WINDOW_SIZE, 65)
    self.bubble_label.hide()  # Ẩn ban đầu

    # ---- Sprite Label ----
    self.sprite_label = QLabel(MOOD_SPRITES["Happy"], self)
    self.sprite_label.setAlignment(Qt.AlignmentFlag.AlignCenter)
    self.sprite_label.setStyleSheet("font-size: 80px; background: transparent;")
    self.sprite_label.setGeometry(0, 0, self.WINDOW_SIZE, self.WINDOW_SIZE)
```

Thêm method `update_mood` và `_auto_hide_bubble` vào class `PetWindow`:

```python
def update_mood(self, status: str, message: str):
    """
    Cập nhật emoji và hiển thị speech bubble với message từ AI.
    Được gọi từ main.py khi BrainHandler trả về kết quả.
    """
    # Đổi Sprite
    emoji = MOOD_SPRITES.get(status, MOOD_SPRITES["Sad"])
    self.sprite_label.setText(emoji)

    # Hiện Speech Bubble
    if message:
        self.bubble_label.setText(message)
        self.bubble_label.show()

        # Tự ẩn sau 8 giây
        if hasattr(self, "_hide_timer"):
            self._hide_timer.stop()
        from PyQt6.QtCore import QTimer
        self._hide_timer = QTimer(self)
        self._hide_timer.setSingleShot(True)
        self._hide_timer.timeout.connect(self.bubble_label.hide)
        self._hide_timer.start(8000)  # 8 giây
```
</action>

<read_first>
- `src/ui/pet_window.py` (để biết chính xác vị trí chèn code, tránh duplicate)
</read_first>

<acceptance_criteria>
- `pet_window.py` chứa `self.bubble_label` là một `QLabel`.
- `bubble_label` có `setWordWrap(True)`.
- `bubble_label` ẩn ban đầu (`.hide()`).
- Method `update_mood(status, message)` tồn tại trong class.
- `update_mood` cập nhật `sprite_label.setText(emoji)`.
- `update_mood` gọi `bubble_label.show()` và `bubble_label.setText(message)`.
- Bubble tự ẩn sau 8 giây qua `QTimer`.
</acceptance_criteria>

## must_haves

- [ ] Khi `update_mood("Angry", "Tập trung đi Tính!")` được gọi: emoji đổi sang 🔴 và bubble hiện text đó.
- [ ] Bubble tự ẩn sau 8 giây mà không crash.
- [ ] Kéo thả pet vẫn hoạt động bình thường khi bubble đang hiển thị.
